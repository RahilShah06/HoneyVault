fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
passlib==1.7.4
bcrypt==4.0.1
PyJWT==2.10.1
pydantic==2.10.4
python-dotenv==1.0.1
# Decoy generation. Only the provider named in LLM_PROVIDER is
# imported at call time, so the unused one is never loaded.
google-genai>=2.0.0
anthropic>=1.0.0
# Behavioural anomaly detection. Also optional: with these absent the app
# runs unchanged and sessions are simply left unscored by the model.
scikit-learn>=1.5.0
joblib>=1.4.0
